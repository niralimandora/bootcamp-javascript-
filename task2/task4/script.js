let per = 82

switch (true) {
    case (per >= 90):
        console.log("A")
        break;
    case (per >= 80. && per < 90):
        console.log("B")
        break;
    case(per >= 70 && per < 80):
    console.log("C") 
    default:
        console.log("Fail")
}